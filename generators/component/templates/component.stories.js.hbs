import React from 'react'
import { storiesOf } from '@storybook/react'

import {{ pascalCase name }} from '.'

storiesOf('{{ titleCase name }}', module).add('default', () => <{{ pascalCase name }} />)
